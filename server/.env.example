# Example environment variables for WanderNest backend
# Copy this to server/.env or server/.env.local and fill values.
# Both .env and .env.local are gitignored; do NOT commit real credentials.

# Server
PORT=5000

# Supabase (server-side only)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# JWT (optional)
JWT_SECRET=

# AI provider selection: groq | xai | ollama | demo
AI_PROVIDER=groq
AI_DEMO_FALLBACK=false
AI_PUBLIC=false

# Groq (free tier) — https://console.groq.com/keys
# Provide one of the below. Prefer environment variables or mounted secret files.
GROQ_API_KEY=
GROQ_TOKEN=
GROQ_API_KEY_FILE=
GROQ_MODEL=llama-3.3-70b-versatile

# XAI (optional)
XAI_API_KEY=
XAI_MODEL=grok-2-latest

# Ollama (optional local free model) — https://ollama.com
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1

# Frontend origin(s) allowed by CORS (comma-separated)
APP_ORIGIN=
