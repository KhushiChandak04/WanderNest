# Example environment variables for WanderNest backend
# Copy this to server/.env and fill values (do NOT commit real credentials).

## Server configuration
# Preferred dev port (falls back to next port if busy)
PORT=5050

## Supabase configuration (server-side)
# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co
# Service role key (server only). Do NOT use anon key here.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

## JWT (optional if you add custom signing)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

## AI integration

# AI Providers
# Prefer GROQ (OpenAI-compatible) if provided; falls back to XAI (Grok)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-70b-versatile
# Alternatively use XAI (Grok)
XAI_API_KEY=
XAI_MODEL=grok-4-latest
AI_DEMO_FALLBACK=false
# x.ai API key (server-side only)
XAI_API_KEY=replace-with-your-xai-api-key
# Optional: override Grok model (e.g., grok-2-latest, grok-2-mini)
XAI_MODEL=grok-2-latest

# AI provider selection: 'groq' (default), 'xai', or 'ollama'
AI_PROVIDER=groq

# Groq (free tier) — https://console.groq.com/keys
GROQ_API_KEY=replace-with-your-groq-api-key
GROQ_MODEL=llama-3.3-70b-versatile
# If using Ollama (free, local): https://ollama.com
# Start Ollama locally and pull a model (e.g., `ollama pull llama3.1`)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1
AI_DEMO_FALLBACK=false
