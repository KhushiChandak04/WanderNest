# Example environment variables for WanderNest backend
# Copy this to server/.env and fill values (do NOT commit real credentials).

# MongoDB connection string (MongoDB Atlas or local). If omitted, the server will
# start but DB-dependent routes will be unavailable.
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/wandernest?retryWrites=true&w=majority

# JWT secret used to sign tokens
JWT_SECRET=your_jwt_secret_here

# JWT token expiry (optional)
JWT_EXPIRES_IN=7d

# Server port (optional; defaults to 5000)
PORT=5000

# AI Providers
# Prefer GROQ (OpenAI-compatible) if provided; falls back to XAI (Grok)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-70b-versatile
# Alternatively use XAI (Grok)
XAI_API_KEY=
XAI_MODEL=grok-4-latest
AI_DEMO_FALLBACK=false
# Rename this file to .env and fill in your key. Never commit your real key.
XAI_API_KEY=replace-with-your-xai-api-key
# Optional: MongoDB connection for other features
# MONGO_URI=mongodb+srv://user:pass@host/db
